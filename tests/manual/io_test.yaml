name: io_verification_pipeline

servers:
  io: servers/io
  data_juicer: servers/data_juicer

pipeline:
  - io.load_jsonl:
      input:
        path: "data/sample.jsonl"
  
  - data_juicer.clean_content: {}
      
  - io.save_jsonl:
      input:
        path: "data/output_result.jsonl"
