name: parquet_support_test

servers:
  dj: servers/data_juicer

pipeline:
  - name: save_parquet
    dj.clean_content:
      input:
        data: [{"text": "Persist this to parquet"}]
  
  - name: load_parquet
    dj.clean_content:
      input:
        data: "$save_parquet" # This will pass a dict with path to a .parquet file
