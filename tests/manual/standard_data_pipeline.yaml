name: standard_data_processing_pipeline

# Multi-domain processing using Zem Ecosystem
servers:
  nemo: servers/nemo_curator
  dj: servers/data_juicer

pipeline:
  - name: step1_clean
    dj.clean_content:
      input:
        data:
          - text: "<html>Luật số: 01/2020/NĐ-CP 😊</html>   về xử phạt hành chính."
          - text: "Văn bản trùng lặp... và trùng lặp"
  
  - name: step2_normalize
    nemo.normalize:
      input:
        data: "$step1_clean"
        normalization: "NFC"

  - name: step3_filter
    dj.refining_filter:
      input:
        data: "$step2_normalize"
        min_len: 20
        alphanumeric_ratio: 0.2
