# Test configuration for Vietnamese OCR
name: vietnamese_ocr_pipeline
correlator: xfmr-zem

servers:
  ocr: servers/ocr

pipeline:
  - name: viet_ocr_test
    ocr.extract_text:
      file_path: "data/vietnamese_ocr.png"
    cache: false

parameters:
  ocr:
    engine: "viet"